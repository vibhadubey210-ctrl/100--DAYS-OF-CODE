//Write a program to swap the first and last digit of a number.
#include <stdio.h>
int main(){
    int num;
    printf("Enter the value of num\n");
    scanf("%d",&num);
    while(num>0){
        int digit=num%10;
        
    }
}